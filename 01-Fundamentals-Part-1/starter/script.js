class deneme {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }
  getAge() {
    console.log(this.age);
  }

  getName() {
    console.log(this.name);
  }

  setName(name) {
    this.name = name;
  }

  setAge(age) {
    this.age = age;
  }
}
